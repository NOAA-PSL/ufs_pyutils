Updates to be made:

(1) Change all instances of %s to .format;

(2) Divide up functions/methods when necessary and add brief comment
    strings;

(3) Include "Keywords" and "Raises" in docstrings for each method;

(4) Make sure module docstrings comply with PEP8 format;

(5) Remove all instances of **kwargs;

(6) Add __all__ where necessary;

(7) Add licensing information to the top of each module.

(8) Cleanup up authorship information; remove __version__ and
__status__;

(9) Update dates;

(10) Remove unneccsarily loaded modules.

###

The following have moved to /ioapps:

boto3_interface.py
grib_interface.py
www_interface.py